# Agent Notes

- On Windows, use `npm.cmd` in `Start-Process`; plain `npm` may open `npm.ps1` in Notepad.
- Unless explicitly specified otherwise, interpret `커밋/푸쉬` as merging the changes into `main` and pushing the remote `main` branch.
